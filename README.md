# @story/eslint-config

## Установка

Установите библиотеку при помощи пакетного менеджера:

```shell
pnpm add -D @story/eslint-config

npm install --save-dev @story/eslint-config

yarn add -D @story/eslint-config
```